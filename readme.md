# Purpose
This repository is for working on the OSRS wiki.  It's intended for working on more complex MediaWiki components like modules and gadgets.

# /rs3
The `/rs3` directory contains reference material from the RS3 wiki.  It mirrors the directory structure of the root folder.

# /globals
The `/globals` directory contains hint files to fix linting errors for missing globals from the MediaWiki context.